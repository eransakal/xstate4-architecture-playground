import axios  from 'axios';

export const useUsersServer = () => {
  return {
    // getUsers: async () => {
    //   const result = await axios.get('/users');

    //   return result.data;
    // }
  }
}